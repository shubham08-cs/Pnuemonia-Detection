[build]
  command = "pip install -r requirements.txt"
  publish = "."

[build.environment]
  PYTHON_VERSION = "3.11.4"

# Note: Streamlit apps are not fully compatible with Netlify's static site hosting
# Consider using Streamlit Cloud, Heroku, or Railway for better results